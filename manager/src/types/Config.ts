type AppConfig = {
  exitAfterFill: boolean;
  exitAfterFillDelay: number;
  riotClientPath: string;
  autoOpenRiotClient: boolean;
  staySignedIn: boolean;
  encryptUsernames: boolean;
};

export default AppConfig;
